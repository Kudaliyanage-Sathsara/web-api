<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /web_api/

    # Redirect all requests to CodeIgniter front controller.
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
